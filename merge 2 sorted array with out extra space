/*
  arr1=[1,5,6]
  arr2=[2,4,7]
  
*/

iterate the 2nd array.
use binary search to append each value
let first = 0;
let last = arr1.length-1;
let mid = Math.round(last/2)
arr2.forEach(val => {
  if(val > arr1[mid]) {
    first=mid
  }
})
